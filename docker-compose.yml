version: "3.9"
services:

  api:
    container_name: api
    build: .
    ports:
      - "3000:3000"
      
  db:
    container_name: postgres
    image: postgres
    environment: 
      POSTGRES_USER=
      POSTGRES_PASSWORD=
      POSTGRES_DB=